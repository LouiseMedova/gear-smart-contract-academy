"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.decodeHexTypes = void 0;
const utils_1 = require("./utils");
const util_1 = require("@polkadot/util");
const utils_2 = require("../utils");
function decodeHexTypes(hexTypes) {
    let { typesFromTypeDef, namespaces } = (0, utils_1.getTypesFromTypeDef)((0, util_1.hexToU8a)(hexTypes));
    const result = {};
    namespaces.forEach((value, key) => {
        const replaced = (0, utils_1.replaceNamespaces)(typesFromTypeDef[value], namespaces);
        result[key] = (0, utils_2.isJSON)(replaced) ? JSON.parse(replaced) : replaced;
    });
    return result;
}
exports.decodeHexTypes = decodeHexTypes;
